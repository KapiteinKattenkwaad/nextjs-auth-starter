# Next Auth
# You can generate a new secret on the command line with:
# openssl rand -base64 32
NEXTAUTH_SECRET=your-nextauth-secret-key-here
NEXTAUTH_URL=http://localhost:3000

# Database
# For development, we use SQLite
DATABASE_URL="file:./dev.db"

# Email (for password reset)
# For development, you can use a test email service like Ethereal
EMAIL_SERVER_HOST=smtp.ethereal.email
EMAIL_SERVER_PORT=587
EMAIL_SERVER_USER=your-ethereal-email-user
EMAIL_SERVER_PASSWORD=your-ethereal-email-password
EMAIL_FROM=noreply@example.com

# Security
# Session expiration time in seconds (default: 30 days)
SESSION_EXPIRY=2592000
# Password reset token expiration time in seconds (default: 24 hours)
RESET_TOKEN_EXPIRY=86400